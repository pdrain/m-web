<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.meilituibian.web.mapper.WxUserMapper">
     <select id="userList" resultType="WxUser">
         SELECT user_id,open_id,user_name,nick_name,shop_name,contact,create_time,user_type,contract_no,parent,number_id,phone,`status`,
         taxpayer_code,business_license,job_title,update_time FROM wx_user WHERE `status`=1 AND job_title>#{jobTitle}
     </select>
</mapper>